#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
   int a[50],br[50];
    int i, max,num,max2;
    scanf("%d", &num);
    for(int i=0; i<num; i++){
        scanf("%d", &a[i]);
    }
    max=a[0];
    for( i=1; i<num; i++){
        if(a[i]>max){
        max = a[i];
    }
}
max2 = a[0];
for(i=0; i<num; i++){
    if(a[i]>max2 && a[i]<max){
        max2 = a[i];
    }
}
printf("%d\n%d", max, max2);
    return 0;
}
